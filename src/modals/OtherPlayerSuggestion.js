import Modal from 'react-modal'

const OtherPlayerSuggestion = () => {

    return (
        <Modal isOpen={true}>
            <div>
            {/* {`player suggested ${selectedSuspect} in the ${selectedRoom} with the ${selectedWeapon}`} */}
            </div>
        </Modal>
    )
}

export default OtherPlayerSuggestion